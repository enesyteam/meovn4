m_admin.controller('DownloadCtrl',
    function($rootScope, $scope, $http, $filter, $timeout, 
        MFirebaseService, MFacebookService, MUtilitiesService, fanpages) {
        // get access token
        // console.log(fanpages);
        alert('working!')
    
});