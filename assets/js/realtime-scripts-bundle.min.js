var realtimeApp = angular.module('mRealtime', ['ngAnimate'])
	.constant('appVersion', '4.0.0')
	.constant('releaseDate', 'Dec-20, 2017')
	// .config(function($stateProvider,$locationProvider, $urlRouterProvider){
 //        $locationProvider.hashPrefix('');
 //        // use the HTML5 History API

 //        $urlRouterProvider.otherwise("/");
 //        $stateProvider
 //          .state('home',{
 //            url: '/',
 //                controller : 'MainCtrl',
 //                templateUrl: "/realtime/index.html"
 //              })
 //    })
	.run(themeRun);

function themeRun($rootScope, appVersion, releaseDate){
	$rootScope.appVersion = appVersion;
	$rootScope.releaseDate = releaseDate;
}
realtimeApp.controller('MainCtrl',
    function($scope, $http, $rootScope, $timeout) {
    	// alert('working!');
    });
